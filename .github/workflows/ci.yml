name: CI

on: [pull_request]

jobs:
  clang-format:
      uses: engine3d-dev/ci/.github/workflows/lint.yml@main
      secrets: inherit
  
  test-yaml:
    uses: engine3d-dev/ci/.github/workflows/test_deploy.yml@main
    with:
      repo: 'experimental-ci'
      version: '1.0'
      run: |
        echo "${{ github.ref_name }}"